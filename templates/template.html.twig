<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{{ title }}</title>
        {% set theme = 'sketchy' %}
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="icon" href="/img/logoIcon.gif"/>
        <link rel="stylesheet" href="/css/{{ theme }}/bootstrap.min.css">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Cookie">
        <link rel="stylesheet" href="/fonts/font-awesome.min.css">
        <link rel="stylesheet" href="/fonts/simple-line-icons.min.css">
        <link rel="stylesheet" href="/css/footer.css">
        {% block stylesheet %}{% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-light navbar-expand-md">
        <div class="container">
            <a class="navbar-brand" href="{{ path("home") }}">
                <img width= "75" height="70" src="/img/logoPoulpe.png"/>
            </a>
            <button class="navbar-toggler" data-toggle="collapse" data-target="#navcol-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navcol-1">
                <form class="form-inline navbar-left" action="{{ path("catalogC", {'cat': app.request.attributes.get('cat')|default('all')}) }}">
                    <div class="input-group">
                        <div class="input-group-prepend">
                            <button class="input-group-text" type="submit">
                                <i class="fa fa-search"></i>
                            </button>
                        </div>
                        <input
                            class="form-control"
                            type="text" name="q"
                            placeholder="Recherche"
                            value="{{ app.request.query.get('q') }}"
                        >
                    </div>
                    {% if app.request.query.get('shortPrice')  %}
                        <input type="hidden" name="shortPrice" value="{{ app.request.query.get('shortPrice') }}">
                    {% endif %}
                    {% if app.request.query.get('stars')  %}
                        <input type="hidden" name="stars" value="{{ app.request.query.get('stars') }}">
                    {% endif %}
                </form>
                <ul class="nav navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path("home") }}">Accueil</a>
                    </li>
                    <li class="dropdown">
                        <a class="dropdown-toggle nav-link" data-toggle="dropdown" aria-expanded="false">Categories</a>
                        <div class="dropdown-menu" role="menu">
                            <a class="dropdown-item" href="{{ path("catalogC", { 'cat': 'all'}) }}">Tous les produits</a>
                            {% for category in entityService.getAllCategorys() %}
                                <a class="dropdown-item" href="{{ path("catalogC", { 'cat': category.name}) }}">{{ category.title }}</a>
                            {% endfor %}
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" style="padding-left: 35px;position:relative" href="{{ path('card') }}">
                            <img style="left:0;height:26px;top:5px;position:absolute" src="/img/panier.png"/>
                            <span class="badge badge-pill badge-primary" style="position:absolute;left:10px;top:5px;font-size:8px;color:white;font-weight:bold;">0</span>Panier
                        </a>
                    </li>
                    <li class="dropdown">
                        <a class="dropdown-toggle nav-link" data-toggle="dropdown" aria-expanded="false" href="#">
                            {% if app.user %}
                                <img src="/img/{% if app.user.avatarUrl %}clients/{{ app.user.avatarUrl }}{% else %}{{ 'defaultavatarurl.png' }}{% endif %}" alt="avatar" style="width:20px;">
                                <span>{{ app.user.login }}</span>
                            {% else %}
                                <span>Compte</span>
                            {% endif %}
                        </a>
                        <div class="dropdown-menu" role="menu">

                            {% if is_granted('ROLE_USER') %}
                                <a class="dropdown-item" href="{{ path("profile") }}">Modifier mon profil</a>
                                <a class="dropdown-item" href="/commands">Historique des commandes</a>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <a class="dropdown-item" href="/admin">Administration</a>
                                {% endif %}
                                <a class="dropdown-item" href="{{ path("logout") }}">Déconnectez-vous !</a>
                            {% else %}
                                <a class="dropdown-item" href="{{ path("login") }}">Identifiez vous !</a>
                                <a class="dropdown-item" href="{{ path("register") }}">Nouveau client !</a>
                            {% endif %}
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="mt-2">
        {% block content %}{% endblock %}
    </div>
    <footer style="background-color:rgb(23,50,77);">
        <div class="row">
            <div class="col-sm-6 col-md-4 footer-navigation">
                <h3 style="margin-top:12%; margin-left:19%; font-size:558%;"><a href="{{ path("home") }}">Ô '<span> Tako</span></a></h3>
            </div>
            <div class="col-sm-6 col-md-4 footer-contacts">
                <div>
                    <span class="fa fa-map-marker footer-contacts-icon" style="background-color:rgb(21,107,159); margin-top:8%;"> </span>
                    <div class="adresse-footer" style="font-size:20px; margin-left:13%; margin-top:-9%;">
                        <span class="new-line-span" style="font-size:90%;">8 rue des tentacules, Avenue des otakus,</span>
                        66000 Octlantis World.
                    </div>
                </div>
                <div><i class="fa fa-phone footer-contacts-icon" style="background-color:rgb(21,107,159); margin-top:4%;"></i>
                    <p class="footer-center-info email text-left" style="margin-top:3%;"> +334 27 86 84 23</p>
                </div>
                <div><i class="fa fa-envelope footer-contacts-icon" style="background-color:rgb(21,107,159);"></i>
                    <p style="margin-top:2%;"> <a href="mailto:Poulpi.OTako@ppe.sio" target="_top">Poulpi.OTako@ppe.sio</a></p>
                </div>
            </div>
            <div class="col-sm-6 col-md-4 footer-about">
                <div>
                    <h4 style="margin-top:10%; font-size:125%;">Pour information : </h4>
                    <p style="margin:4%; font-size:90%;"><a href="{{ path("legal") }}">Mentions légales et conditions générales de vente</a></p>
                    <p style="margin:4%; font-size:90%;"> Octlantis est une ville sous-marine construite par des poulpes !
                        <br> Plus d'infos ici -->
                        <a href="https://www.lexpress.fr/actualite/sciences/voici-octlantis-une-ville-sous-marine-construite-par-des-poulpes_1944982.html">Octlantis 🐙</a>
                    </p>
                </div>
            </div>
        </div>
    </footer>
    <script src="/js/jquery-3.3.1.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
